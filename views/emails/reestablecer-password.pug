<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer Contraseña
        p Hola, has solicitado reestablecer tu contraseña, has click en el siguiente enlace para reestablecerlo. Este enlace es temporal, si vence vuelve a solicitarlo
        a(href=`${resetUrl}`) Ir a Resetear Contraseña
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste esta opción, puedes ignorarlo