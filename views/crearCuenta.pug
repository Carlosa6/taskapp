<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en UpTask

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="fullname") Nombre
                    input(type="text" name="fullname" placeholder="Nombres Completos" value= fullname required)
                .campo
                    label(for="email") Correo Electrónico
                    input(type="email" name="email" placeholder="Correo Electrónico" value= email required)
                .campo
                    label(for="password") Contraseña
                    input(type="password" name="password" placeholder="Contraseña" value= password required)
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a Iniciar Sesión

        script(src="/dist/bundle.js")